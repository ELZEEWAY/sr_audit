// preload.js
// Optional preload script to safely expose API wrappers to renderer
window.addEventListener('DOMContentLoaded', () => {
  console.log('SR-AUDIT SYSTEM Electron Desktop Shell initialized.');
});
